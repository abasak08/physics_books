This repo is a git-lfs repo. To git clone it you need lfs in your system. 

For debian based OS:
$ sudo apt install git
$ sudo apt install git-lfs
$ git clone https://github.com/abasak08/physics_books.git
$ cd physics_books
$ git lfs install
$ git lfs pull
